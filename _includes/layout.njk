<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title%}{{ site.title }}{% endblock %}</title>

    <link rel="stylesheet" href="{{'/styles/style.css' | prefixedUrl }}">
    <link rel="stylesheet" href="{{'/styles/nav.css' | prefixedUrl }}">
    <link rel="stylesheet" href="{{'/styles/home.css' | prefixedUrl }}">


</head>
<body>
    {% include "nav.njk" %}

    <main>
        {% block content %}
            {{ content | safe }}
        {% endblock %}

        <h2>Latest Posts</h2>
            <ul>
                <!-- loops through all of our md files with the tag 'post' --> 
            {% for post in collections.post %}
            <li><a href="{{ post.url | prefixedUrl }}">{{ post.data.title }}</a></li>
            {% endfor %}
            </ul>
    </main>
    {% include "footer.njk" %}
</body>
</html>