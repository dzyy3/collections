{
  "name": "collections",
  "version": "1.0.0",
  "description": "Eleventy site configured for GitHub Pages (Windows CMD)",
  "scripts": {
  "clean": "rimraf docs",
  "build": "npm run clean && cross-env ELEVENTY_ENV=production npx @11ty/eleventy",
  "serve": "npx @11ty/eleventy --serve"
},
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "cross-env": "^10.1.0",
    "luxon": "^3.0.0",
    "rimraf": "^6.0.1"
  }
}
